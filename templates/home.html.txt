{% extends 'base.html' %}

{% block content %}
<div class="row">
    <div class="col-md-4 mb-4">
        <div class="card shadow-sm border-0 bg-dark text-white text-center p-4">
            <h3>Bienvenue !</h3>
            <p class="text-muted">Plateforme de Gestion de Tâches</p>
            <hr class="border-secondary">
            <div class="d-flex justify-content-around mt-3">
                <div>
                    <h4 class="fw-bold text-primary">0</h4>
                    <small class="text-muted">Projets</small>
                </div>
                <div>
                    <h4 class="fw-bold text-warning">0</h4>
                    <small class="text-muted">Tâches</small>
                </div>
            </div>
        </div>
        
        <div class="list-group shadow-sm mt-4">
            <a href="/projet/creer/" class="list-group-item list-group-item-action fw-bold text-success">
                ➕ Créer un nouveau projet
            </a>
            <a href="/tache/creer/" class="list-group-item list-group-item-action fw-bold text-primary">
                📝 Assigner une nouvelle tâche
            </a>
        </div>
    </div>

    <div class="col-md-8">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2 class="h4 fw-bold text-secondary">Mes Espaces de Travail (Projets)</h2>
        </div>

        {% if taches %}
            {% else %}
            <div class="card text-center p-5 border-0 shadow-sm bg-white">
                <div class="card-body">
                    <h5 class="text-muted mb-3">Aucun projet en cours pour le moment.</h5>
                    <p class="small text-secondary mb-4">Commencez par créer un projet collaboratif et invitez les membres de votre équipe pour suivre l'avancement des tâches.</p>
                    <a href="/projet/creer/" class="btn btn-primary px-4 py-2">Créer mon premier projet</a>
                </div>
            </div>
        {% endif %}
    </div>
</div>
{% endblock %}